non-program question.

provide another stack.

**when push**
```
if (new element <= min ()){    
    newStack.push_back(new element)    
}
```

**when pop**
```
if (popped value == min()){    
    newStack.pop();    
}
```

**when min**
```
if empty ,return some value
else return newStack[-1]
```
